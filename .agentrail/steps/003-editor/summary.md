Added code editor component with C syntax highlighting using the overlay technique (transparent textarea over highlighted pre). Created highlight.rs with a tokenizer that colors keywords (mauve), types (blue), numbers (peach), strings (green), comments (gray), preprocessor directives (red), and function calls (yellow) in the Catppuccin Mocha palette. Created editor.rs as a reusable Yew component with scroll sync between layers. Verified in browser — highlighting renders correctly and compile-and-run still works.